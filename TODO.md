# TODO

## Аналитика

- [ ] **Яндекс.Метрика.** Счётчик подключать в `base-layout.astro` с защитой от пререндера:
      на сайте включены Speculation Rules, и без проверки `document.prerendering` +
      события `prerenderingchange` Метрика запишет просмотры страниц, которые никто не открывал.
      Включить вебвизор и карту кликов: важно видеть, какие блоки формы раскрывают
      (ставка по периодам, отсрочка, досрочки) и докручивают ли до таблицы.
- [ ] **Google Analytics 4.** Сам откладывает отправку до активации пререндера, защита не нужна.
- [ ] **Собственные события.** Отправлять в обе системы: выбор пресета, добавление периода
      ставки / отсрочки / досрочки, переключение типа платежей, нажатия «Скопировать ссылку»,
      «Скачать CSV», «Печать», переход к сравнению, число сценариев на странице сравнения.
      Точки подключения — обработчики в `lib/calculator-ui.ts` и `lib/compare-ui.ts`.
- [ ] **Search Console и Яндекс.Вебмастер.** Добавить сайт, загрузить `sitemap.xml`, положить
      файлы подтверждения в `public/`, следить за индексацией страниц `/mortgage/*`.
- [ ] Если аналитика собирает персональные данные (IP считается) — страница политики
      конфиденциальности и уведомление о cookies по законодательству РБ.

## Деплой

- [ ] Репозиторий на GitHub: для превью на Pages он должен называться `<владелец>.github.io`
      (как у metallomaster — через организацию), иначе прогон превью пропускается. Ветка
      `main` → domain.by, ветка `dev` → GitHub Pages. Workflow уже лежат в
      `.github/workflows/production.yml` и `preview.yml`, настройка — в README.
- [ ] SSH-доступ на хостинге, ключ деплоя, секреты `SSH_HOST`, `SSH_PORT`, `SSH_USER`,
      `SSH_PRIVATE_KEY`, переменная `DEPLOY_ENABLED`, проверить `DEPLOY_PATH` в workflow.
- [ ] Settings → Pages → Source → GitHub Actions; затем Settings → Environments →
      `github-pages` → Deployment branches → добавить `dev`, иначе превью с этой ветки
      отказывается деплоиться. По желанию: `production` → Required reviewers, чтобы выкат
      ждал подтверждения.
- [ ] После первого выката проверить: `curl -I http://calcwise.by/` → 301 на https,
      `www` → без www, `/nope/` → 404, заголовки безопасности из `.htaccess` на месте.
- [ ] Хостинг для `.by` у физического лица может быть где угодно; для юрлица или ИП сайт
      должен размещаться в РБ (Указ № 60). domain.by этому требованию соответствует.

## Продукт

- [ ] **Новые разделы.** Сайт задуман как набор калькуляторов для разных видов кредитования,
      а не только ипотека и кооператив: лизинг (авто, оборудование, недвижимость), потребительский
      кредит, кредитная карта, рассрочка и другие варианты, которые появятся у владельца.
      Навигация, футер и sitemap должны расти без правок по всему проекту — по образцу
      `lib/menu.ts` и `lib/mortgage-pages.ts`.
- [ ] **Раздел «Кооператив».** Ждёт модели от владельца: паи, вступительный, членский и
      ежемесячные взносы, момент перехода ко второму этапу. Черновик формул —
      `reference/cooperative-calculator.js`.
- [ ] **Раздел «Лизинг».** Модель от владельца: аванс, срок, удорожание или ставка,
      выкупная стоимость, НДС в платеже; график и переплата в том же виде, что у ипотеки.
- [ ] Даты платежей и проценты по фактическому числу дней (365/366) — для построчной сверки с
      договором. Владелец решил пока не делать.
- [ ] Экспорт в `.xlsx`: сейчас CSV с `;` и десятичной запятой, открывается в Excel без диалога.
- [ ] Печатная версия: проверить разбиение таблицы на страницы в Safari.
- [ ] Иконки приложения сделать нейтрального цвета, чтобы и на тёмной, и на светлой теме они
      выглядели хорошо.
- [ ] Учитывать инфляцию.
- [ ] Пресеты ставок убраны по решению владельца: условия программ меняются, поддерживать их не
      хочется, пользователь настраивает всё сам.

## Проверить перед запуском

- [ ] Lighthouse по всем шаблонам страниц (скилл `web-quality-audit`), цель ≥ 95 везде.
- [ ] Rich Results Test: WebApplication, FAQPage, BreadcrumbList, Article.
- [ ] Реальные устройства: iPhone Safari (таб-бар, safe-area), Android Chrome.

## Открытые вопросы к владельцу

- [ ] Строка «0» в банковском расчётнике с платежом 3 396,21 без долга и процентов: нужны первые три
      и последние две строки графика банка с датами и итогом, чтобы понять — информационная строка
      или авансовый платёж в день выдачи. Наша переплата с банком совпала до копейки, поэтому
      лишнего платежа в графике банка нет.

- [ ] seo
- [ ] печать
- [ ] delete scripts
- [ ] delete references
- [ ] Добавить форму с обратной связью, если есть какие-то ошибки
